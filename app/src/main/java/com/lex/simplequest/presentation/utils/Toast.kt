package com.lex.simplequest.presentation.utils

import android.content.Context
import android.content.res.ColorStateList
import android.widget.TextView
import android.widget.Toast
import androidx.annotation.StringRes
import androidx.core.content.ContextCompat
import com.lex.simplequest.R

object ApplicationToast {

//    fun makeText(context: Context, @StringRes resId: Int, duration: Int): Toast =
//        Toast.makeText(context, resId, duration).apply { applyApplicationStyle(context) }
//
//    fun makeText(context: Context, text: CharSequence, duration: Int): Toast =
//        Toast.makeText(context, text, duration).apply { applyApplicationStyle(context) }
//
//    private fun Toast.applyApplicationStyle(context: Context) {
//        this.view.backgroundTintList =
//            ColorStateList.valueOf(ContextCompat.getColor(context, R.color.toast_background_color))
//        this.view.bindOrNull<TextView>(android.R.id.message)
//            ?.setTextColor(ContextCompat.getColor(context, R.color.toast_text_color))
//    }
}